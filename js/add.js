$(document).ready(function () {
	/*var $inputs = $("#add-news input, #add-news textarea");
	var check = true;
	function checkInput () {
		for (var i = 0; i < $inputs.length; i++) {
			if (!$inputs.eq(i).val()) {
				$("#save").prop("disabled", true);
				check = false;
			} else {
				if (i == $inputs.length - 1) {
					check = true;
				}
			} 
			if (check) {
				$("#save").prop("disabled", false);
			}
		};
	}
	checkInput();
	$inputs.on("blur", checkInput);*/
});